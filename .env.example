# Copy this file to .env and set a password:
#   cp .env.example .env && openssl rand -hex 32
# Use hex output: the password is part of the DATABASE_URL connection URI,
# where ':', '@' and '/' have structural meaning.
# POSTGRES_PASSWORD and the password inside DATABASE_URL must match.


POSTGRES_USER=recipe
POSTGRES_PASSWORD=
POSTGRES_DB=recipe
POSTGRES_PORT=5432

DATABASE_URL="postgresql://recipe:SET_YOUR_PASSWORD@localhost:5432/recipe?schema=public"
